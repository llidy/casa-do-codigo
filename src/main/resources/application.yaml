spring:
  datasource:
    url: jdbc:mysql://localhost:3307/casa_do_codigo?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: admin
  jackson:
    default-property-inclusion: non_null
  properties:
    jpa.hibernate.format_sql: true
    jpa.show-sql_sql: true


